services:
  togetherplay:
    build: /mnt/user/appdata/togetherplay
    container_name: togetherplay
    ports:
      - "3000:3000"
    environment:
      - DATA_DIR=/data
      - ADMIN_PASSWORD=changme
    volumes:
      - /mnt/user/appdata/togetherplay/data:/data
    restart: unless-stopped