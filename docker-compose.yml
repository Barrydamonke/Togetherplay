services:
  togetherness:
    build: /mnt/user/appdata/togetherness
    container_name: togetherplay
    ports:
      - "3000:3000"
    environment:
      - DATA_DIR=/data
      - ADMIN_PASSWORD=changme
    volumes:
      - /mnt/user/appdata/togetherness/data:/data
    restart: unless-stopped